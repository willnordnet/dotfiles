# Claude Code Dotfiles

Shared Claude Code configuration: global instructions, skills, hooks, and settings. The install script symlinks everything into `~/.claude/` so updates from the repo are picked up automatically.
